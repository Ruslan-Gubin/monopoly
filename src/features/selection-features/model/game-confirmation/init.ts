import { gameConfirmationInit } from "./types";


const initialState: gameConfirmationInit = {
  isModalActive: false,
  players: [],
  sessionId: null,
  playerColor: [],
  selectedColor: null,
}

export { initialState }